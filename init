echo ">> Init/update git submodule(s)"
git submodule init
git submodule update

exports=(adb bashrc emacs gitconfig irssi pythonrc rin.sh vim vimrc)
echo ">> Link ${#exports} elements."
for i in ${exports[*]};do
    echo $i && ln -s "$HOME/.my/$i" "$HOME/.$i"
done

